# 前端迁移说明

## 📋 变更概述

本次更新将项目前端从**静态 HTML/CSS/JS** 升级为基于 **React + TypeScript + Vite** 的现代化单页应用。

---

## ✨ 主要变化

### 1. 技术栈升级

| 项目 | 旧版 | 新版 |
|------|------|------|
| **框架** | 原生 HTML/JS | React 18.3.1 + TypeScript |
| **构建工具** | 无（直接运行） | Vite 6.3.5 |
| **样式方案** | 原生 CSS | Tailwind CSS + CSS Modules |
| **UI 组件** | 手写 | Radix UI（无障碍组件库） |
| **动画** | CSS Transitions | Motion (Framer Motion) |
| **图标** | SVG 内联 | Lucide React |

### 2. 文件结构变化

**旧版（已删除）：**
```
apps/ui/
├── index.html    # 单一 HTML 文件
├── script.js     # 所有逻辑
└── styles.css    # 所有样式
```

**新版：**
```
apps/ui/
├── index.html              # HTML 入口（极简）
├── package.json            # 依赖管理
├── vite.config.ts         # 构建配置
├── README.md              # 前端文档
├── .gitignore             # Git 忽略规则
└── src/                   # 源代码目录
    ├── App.tsx            # 主应用逻辑
    ├── main.tsx           # React 入口
    ├── index.css          # 基础样式
    ├── components/        # 组件目录
    │   ├── Landing.tsx
    │   ├── PersonaBuilder.tsx
    │   ├── JobRecommendations.tsx
    │   ├── JobSearch.tsx
    │   ├── ResumeGeneration.tsx
    │   └── ui/            # 基础 UI 组件库（40+ 组件）
    └── styles/
        └── globals.css    # 全局样式
```

### 3. 新增功能

✅ **主题系统**
- 深色模式 / 浅色模式切换
- 流畅的主题过渡动画

✅ **国际化**
- 中文 / 英文语言切换
- 完整的双语界面

✅ **动画效果**
- 页面切换动画（Apple 风格）
- 交互式背景效果
- 组件加载动画

✅ **交互式元素**
- PopCat 宠物猫陪伴
- 实时进度条
- 响应式布局

✅ **现代化组件**
- 高质量 UI 组件（来自 Radix UI）
- 完全的键盘导航支持
- ARIA 无障碍标准

---

## 🚀 如何启动

### 方法 1：使用批处理脚本（最简单）

**启动前端：**
```cmd
双击 启动前端.bat
```

**启动后端：**
```cmd
双击 启动后端.bat
```

### 方法 2：手动启动

**前端：**
```powershell
cd apps/ui
npm install          # 首次运行
npm run dev          # 启动开发服务器
```
前端地址：<http://localhost:3000>

**后端：**
```powershell
.\.venv\Scripts\Activate.ps1
uvicorn apps.api.main:app --reload --port 8000
```
后端地址：<http://localhost:8000>

---

## 📦 依赖安装

### 首次运行前必须安装依赖

```powershell
cd apps/ui
npm install
```

### 核心依赖列表

- `react` & `react-dom` - React 核心库
- `motion` - 动画库（基于 Framer Motion）
- `lucide-react` - 图标库
- `@radix-ui/*` - 无障碍 UI 组件（40+ 包）
- `class-variance-authority` - 组件变体管理
- `clsx` & `tailwind-merge` - 样式工具
- `vite` - 构建工具
- `@vitejs/plugin-react-swc` - React 快速刷新

完整依赖列表请查看 `apps/ui/package.json`。

---

## 🔗 API 集成

新前端会自动连接到本地后端 API（`http://localhost:8000`）。

如需修改 API 地址，请在各组件中查找类似代码：

```typescript
const API_BASE = 'http://localhost:8000';
```

---

## 🎨 用户界面预览

### 应用流程

1. **欢迎页** → 点击开始
2. **Step 1** → 上传简历或输入信息
3. **Step 2** → 查看 AI 推荐的职位
4. **Step 3** → 搜索更多职位
5. **Step 4** → 生成定制简历并导出

### 特色亮点

- 🎭 主题切换按钮（右上角）
- 🌐 语言切换按钮（右上角）
- 🐱 交互式小猫（左下角，点击有惊喜）
- 📊 步骤进度条（底部）
- ✨ 流畅的背景动画

---

## 📖 开发文档

- **前端详细文档**：`apps/ui/README.md`
- **运行指南（含前后端）**：`docs/运行指南.md`
- **API 文档**：启动后端后访问 <http://localhost:8000/docs>

---

## ⚠️ 注意事项

### 1. 旧版静态页面已删除

原来的 `apps/ui/index.html`、`script.js`、`styles.css` 已被完全替换。

如需查看旧版代码，请使用 Git 恢复：
```bash
git checkout HEAD~1 -- apps/ui/index.html
git checkout HEAD~1 -- apps/ui/script.js
git checkout HEAD~1 -- apps/ui/styles.css
```

### 2. 需要 Node.js 环境

新版前端**必须**安装 Node.js 才能运行。

推荐版本：Node.js 18+ 和 npm 9+

下载地址：<https://nodejs.org/>

### 3. 开发服务器端口

- 前端默认端口：`3000`
- 后端默认端口：`8000`

如有冲突，可在 `apps/ui/vite.config.ts` 中修改前端端口。

### 4. 构建生产版本

```bash
cd apps/ui
npm run build
```

构建产物将输出到 `apps/ui/build/` 目录。

---

## 🐛 常见问题

### Q: npm install 失败怎么办？

**A:** 尝试清除缓存：
```bash
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### Q: 页面显示空白？

**A:** 检查：
1. 后端 API 是否正常运行（<http://localhost:8000/docs>）
2. 浏览器控制台是否有错误
3. 依赖是否安装完整（`npm install`）

### Q: 动画卡顿怎么办？

**A:** 
- 关闭浏览器的硬件加速限制
- 更新显卡驱动
- 使用 Chrome/Edge 等现代浏览器

### Q: 如何禁用宠物猫？

**A:** 在 `src/App.tsx` 中注释掉 PopCat 组件：
```typescript
// <PopCat isDark={isDark} />
```

---

## 🎯 下一步

1. ✅ 熟悉新界面和功能
2. ✅ 查看 `apps/ui/README.md` 了解开发细节
3. ✅ 测试前后端集成是否正常
4. 🔲 根据需求自定义样式和功能
5. 🔲 添加更多页面和组件

---

## 💡 技术支持

遇到问题？参考以下资源：

- **前端文档**：`apps/ui/README.md`
- **运行指南**：`docs/运行指南.md`
- **React 文档**：<https://react.dev/>
- **Vite 文档**：<https://vitejs.dev/>
- **Radix UI**：<https://www.radix-ui.com/>

---

**祝使用愉快！** 🚀

